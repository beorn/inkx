---
mentions:
  - km
  - claude
id: "@km/session/0221c"
aliases:
  - km-session.0221c
  - km-session-0221c
created_by: claude:4c413aae
created_at: 2026-02-21T19:09:14Z
closed_at: 2026-02-23T00:57:06Z
owner: bjorn@stabell.org
assignee: claude:4c413aae
---

# [x] Session: kmast completion + tree architecture discussion @km/session #task #P2 @claude:4c413aae

## Session: kmast completion + tree architecture

### Completed

1. **kmast heading-task-mark transform** — headings now go through same task mark extraction as list items
2. **kmInlinePropTransform extended to headings** — paragraphs AND headings get props/propsRaw/cleanText
3. **interpretHeadingRules reads from propsRaw** — replaced parseHeadingRules(text) with interpretHeadingRules(propsRaw) in astToNodes, eliminating last text re-parsing
4. Duplicate km.add:: concatenated with comma — propsRaw["km.add"] = "query1, query2" instead of last-wins
5. km.default default — first non-collapsed, non-removed column is default; km.default:: true only needed as override
6. **62 new integration tests** — kmast-integration.test.ts covering data fields, combined features, heading rules, roundtrip
7. **Pipeline audit** — confirmed all paths go through kmast, no legacy regex extraction remaining
8. **Docs updated** — kmast.md, tasks.md, templates (@next.md, init.ts)

### Architectural Issues Raised

1. **Flat nodes vs tree** — km uses flat KNode[] with parent_id refs; user wants proper tree structure like an outliner
2. **Invalid heading nesting** — H3 inside H1 (no H2) works but is fragile; Decker auto-demotes skip-level headings
3. **km is an outliner** — the data model should reflect this: proper nested tree, not adjacency list
4. **parseHeadingRules still exported** — used in 2 test files and parser.ts; could be internalized further
5. **extractTitleTaskMarker still used in TUI** — tree-node-helpers.tsx re-extracts from content text; should read from KNode fields instead

