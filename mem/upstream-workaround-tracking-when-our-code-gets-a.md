## Upstream Workaround Tracking When Our Code Gets A @memory

Upstream workaround tracking: when our code gets a workaround for an upstream bug, file a bead under km-all.upstream-waiting via .claude/skills/pm/workflows/upstream.md §8 (template + unwind protocol there). Reviewed monthly via /sop infra upstream-waiting check. NOT optional when a workaround exists — without it, workarounds become permanent silent debt.
