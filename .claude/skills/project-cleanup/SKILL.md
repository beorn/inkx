---
description: "Redirected to /sop. Use /sop infra instead."
---

# Project Cleanup -> Absorbed into /sop

This skill has been absorbed into the SOP framework.

| Old Command | New Command |
|-------------|-------------|
| `/project-cleanup <path>` | `/sop infra` |
| `/project-cleanup --deep <path>` | `/sop infra` |
| `/project-cleanup --report-only <path>` | `/sop infra --scan-only` |

Sub-files (`heuristics.md`, `patterns.md`) remain as reference material.

Run `/sop` for the unified maintenance framework.
