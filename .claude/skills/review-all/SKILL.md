---
description: "Redirected to /sop. Use /sop --scan-only instead."
---

# Review-All -> Absorbed into /sop

This skill has been absorbed into the SOP framework.

| Old Command | New Command |
|-------------|-------------|
| `/review-all` | `/sop --scan-only` |
| `/review-all code` | `/sop code --scan-only` |
| `/review-all code,tests` | `/sop code --scan-only` |
| `/review-all --skip=seo` | `/sop --scan-only` (select domains explicitly) |

Run `/sop` for the unified maintenance framework.
