{
  "modules": ["release", "verify", "format-check", "oxfmt"],
  "vars": { "testCommand": "bun run test", "bunVersion": "1.3.11" },
  "overrides": {
    "release": "multi-package build:all + verdaccio verify-publishable gate before publish — generic single-publish template doesn't apply",
    "verify": "verdaccio-registry pack+install+import gate (legacy npm-install variant hit ETARGET on unpublished @silvery/* deps during release windows)",
    "format-check": "gate runs `bun run fix` = oxlint --fix + oxfmt — asserts MORE than the oxfmt-only template; do not weaken",
    "oxfmt": "minimal config (semi only) predates canonical; mass reflow deferred — highest-churn vendor repo, would conflict with in-flight branches"
  }
}
