
## Session 227d13d3 (12:14)

- Decisions made and why
  - Add suppress: true to patchConsole in view.ts (patchConsole(console, { capture: true, suppress: true })) to ensure console output is captured for the UI and not dumped to the terminal. Reason: -vv debug logs were flooding the terminal and breaking the TUI.
  - Add suppress: true to fallback patch in tui.tsx as well, for the same reason (to cover cases where patchConsole isn’t provided).
  - Fix toast UX: Change useLogToast in board-bottom-bar.tsx to fire only once (one-shot) when the log total becomes non-zero, not on every update. Reason: verbose logs produced hundreds/thousands of toasts, obscuring the UI.
  - Keep progress rendering separate: Acknowledge that progress UI uses direct stdout writes and is unaffected by console suppression; suppression targets console.* logs, not process.stdout paths.

- Bugs found and root causes
  - Console flood in TTY: writeLog() uses console.debug and patchConsole with capture only; suppress did not apply, so logs still reached the terminal.
  - Toast flood: useLogToast fired on every consoleStats.total update, so high-frequency log streams produced many toasts covering the UI.
  - Import alias confusion (pre-refactor): view.ts had two imports aliasing the same createLogger function, indicating a rename/refactor drift that didn’t change behavior.

- Approaches that failed and why
  - Relying solely on capture without suppress: capture alone did not stop console.* from writing to the terminal, so the TUI UI remained compromised.
  - Debouncing toasts without changing trigger logic: Debounce helped some UI components but not when the underlying signal (consoleStats.total) updates extremely rapidly; users still saw floods.
  - Assuming progress UI would be affected by console suppression: progress renders via process.stdout, not console.*, so suppression needed to be scoped to console.*.

- Architectural patterns or conventions discovered
  - Console patching pattern: using patchConsole with capture and suppress to route console messages into a Console component within the UI, keeping the terminal clean.
  - Clear separation of concerns: console logs (captured and replayed in UI) vs. direct stdout progress rendering (unaffected by console patching).
  - UI feedback best practices under high-frequency logs: prefer one-shot notifications or throttled updates rather than per-event to avoid UI clutter.

- Warnings for future sessions
  - Verify patchConsole usage comprehensively: ensure suppress is set where needed; default behaviors can be surprising.
  - Be cautious with log-driven UI: high-frequency logs can overwhelm toasts; implement one-shot or proper throttling for log-related UI notifications.
  - If enabling verbose logs, validate both TUI stability and user experience (avoid masking real UI state with flood of messages).
  - Understand the boundary between console-based logs and progress rendering to avoid unintended suppression effects on non-console outputs.
  - Submodule and repo state: monitor in-progress merges (AUTO_MERGE) as they can affect debugging context.

## Session a3625ec3 (12:27)

- Decisions made and why
  - Eliminate InputLayerProvider in L3; route all input through term:key and when predicates instead. Reason: InputLayerProvider requires React contexts (EventsContext/InputContext) not provided by createApp, leading to an incompatibility between the React input hooks and the non-React event loop.
  - Keep option to bridge with a standard InputLayerProvider in createApp (Option C) as a bridge between L1 and L3. Reason: preserves a potential path to reuse InputLayerProvider if contexts can be wired into createApp, reducing future friction.
  - Absorb km-inkx.driver-migrate into L2/L3; reason: consolidate driver changes into the core bead work to avoid drift and divergence.
  - Plan for an adapter-bridge first, then rewrite action handlers in the same PR; reason: incremental risk reduction and smoother integration.
  - Beads-based planning (L1 → L2 → L3 with ready/blocked status); reason: clarifies dependencies and sequencing for large architectural changes.

- Bugs found and root causes
  - Incompatibility between InputLayerProvider and createApp due to missing contexts (EventsContext/InputContext). Root cause: React-based input wiring assumes contexts that the non-React createApp does not provide; not a code bug per se, but a design/context wiring mismatch.

- Approaches that failed and why
  - Relying on InputLayerProvider without establishing required React contexts. Why it failed: creates a mismatch between React input hooks and the non-React event loop in createApp, causing routing and input handling to be brittle or unavailable.
  - (No other concrete failed approaches documented; instead, the session converged on eliminating the provider or bridging it via a proper context setup.)

- Architectural patterns or conventions discovered
  - Bead-based, dependency-ordered planning (L1 → L2 → L3) with explicit readiness/blocked states.
  - Centralized input routing pattern: discrete keys flow through term:key → processKeyWithContext → when predicates → store via set.
  - Clear separation between input modalities:
    - Discrete keys are routed via the command system.
    - Text input suggested to route through a TextInputProvider and useTextInput hook, with term:key deciding when to route to text input channels.
  - Potential adapter bridge pattern (Option C) to connect React-based input providers with the non-React app loop, rather than forcing one side to fit the other.

- Warnings for future sessions
  - Be wary of mismatches between React-based input providers and non-React event loops; ensure necessary contexts are wired if you plan to use a provider like InputLayerProvider.
  - If choosing to remove a React input provider, ensure a robust centralized routing path exists (term:key + when predicates) and consider introducing a dedicated TextInputProvider to handle text input cleanly.
  - When planning large architectural changes, use phased plans with beads and explicit dependencies to manage risk and interruptions.
  - If bridging is pursued (Option C), ensure createApp can expose or supply all needed contexts to support input integration without breaking the existing non-React event flow.

## Session c2195c81 (12:49)

- Decisions made and why
  - Auto-recall on every non-trivial prompt via the UserPromptSubmit hook to keep Session Memory up-to-date without manual triggers.
  - Provide manual recall with flexible CLI flags (--since, --raw, --json, --limit) to enable targeted investigations when needed.
  - Use a two-stage recall pipeline: fast FTS5 search plus cheap LLM synthesis (gpt-5-nano, context.001) to produce actionable summaries quickly and cost-effectively.
  - Architectural separation: hooks are thin wrappers that delegate to subcommands (hook vs remember) to keep concerns modular and testable.
  - Broad recall coverage by indexing ~350K messages across multiple content types (messages, plans, summaries, todos, session first-prompts) for richer memory.
  - Health-focused governance: recall review to monitor index health, hook config, search quality, and synthesis quality.

- Bugs found
  - NONE reported in the transcript.

- Approaches that failed and why
  - NONE described in the transcript.

- Architectural patterns or conventions discovered
  - Hook-based memory integration: UserPromptSubmit and SessionEnd hooks feed memory extraction.
  - Modular command design: memory extraction logic implemented as subcommands invoked by thin wrappers.
  - Retrieval-then-synthesis pattern: primary retrieval via FTS5, followed by LLM-driven synthesis to produce actionable memory.
  - Multi-content indexing: memory span includes messages, plans, summaries, todos, and session prompts to improve recall depth.
  - Observability and control: built-in review/diagnostics (index health, hook configuration, search quality, synthesis) and programmable outputs (JSON, raw, etc.).

- Warnings for future sessions
  - Latency and cost variability: recall latency ranges 3–25 seconds depending on LLM response time; plan for variability.
  - Index freshness and staleness risk: memory accuracy depends on up-to-date indexing; monitor with periodic recalls and health checks.
  - Hook reliability: ensure hooks are installed and executable; misconfigurations can halt recall.
  - Potential memory drift: regular reviews recommended to detect drift between memory and actual state; rely on recall review to mitigate.
  - Resource budgeting: LLM usage has cost implications; consider using cheaper models or scoped queries when possible.

## Session 8f9fb274 (12:57)

- Decisions made and why
  - Lazy loading of skills and not reinjecting workspace files every turn (Claude Code) to reduce per-turn payload and preserve cache prefix stability.
  - Adopt memory + compaction approach to bound context growth while preserving key facts; filesystem acts as ultimate, restorable context to support reconstruction.
  - Treat KV-cache hit rate as the primary cost driver, not raw token count; cache locality determines cost efficiency.
  - Keep prompt prefix stable; changes invalidate downstream cache and force full re-computation.
  - Prefer tool masking over removing tools to avoid cache invalidation from tool-definition changes.

- Bugs found and root causes
  - Overly simplistic cost model (quadratic growth) when not accounting for cache hits/misses and prefix consistency; root cause: ignoring cache semantics in cost reasoning.
  - Cache invalidation when workspace files change (OpenClaw approach) leading to large downstream cost spikes; root cause: per-turn loading of multiple workspace files breaks cache prefixes.
  - Misinterpretation of “full context sent every turn” without distinguishing cache-hit vs cache-miss; root cause: conflating token volume with actual cost dynamics.

- Approaches that failed and why
  - No management strategy: quadratic cost growth due to unchecked context growth and lack of caching optimization.
  - Aggressive compaction: saves tokens but erodes detail and continuity, harming memory usefulness.
  - Per-task sessions: maintain isolation but lose continuity and context linkage across turns.
  - Frequent changes to tool definitions/prompt prefix: triggers cache invalidation and costly recomputations.

- Architectural patterns / conventions discovered
  - KV-cache as a core metric for cost and performance; cache-hit discipline is central.
  - Stable prompt prefix as a design constraint to maximize cache effectiveness.
  - Tool masking as a strategy to preserve cache while evolving capabilities.
  - Filesystem-as-context approach: compress history but keep it restorable; URL/history retained even if content drops.
  - Append-only context philosophy: avoid mutating past summaries; add new insights while preserving prior state.
  - Recitation techniques to keep goals in recent attention and reduce “getting lost” in long sessions.
  - Error retention in context: keep failed actions visible so the model can learn from them rather than trimming them away.
  - Context diversity: vary serialization templates and phrasing to avoid monotone patterns and hallucination.
  - Expectation that long sessions involve roughly ~50 tool calls per task; emphasizes need for attention and context management.

- Warnings for future sessions
  - Do not frequently alter workspace assets or tool definitions; such changes can invalidate large portions of the cache.
  - Always evaluate cost via cache-hit rate, not just total token count; a high hit rate dramatically lowers effective cost.
  - Maintain a stable prompt/tooling prefix to maximize cache reuse; plan changes with cache in mind.
  - Favor append-only history updates over in-place mutations to preserve cache integrity and recoverability.
  - Leverage error retention and diverse context strategies to improve robustness and reduce repeated failures/hallucinations.
  - Treat context engineering as a primary design concern; model capability is limited by how well context is managed.

## Session c135cede (13:07)

- Decisions made and WHY
  - Harden error logging for recall hook: log to stderr with “[recall hook] FATAL: …” including full error, stack trace, and timing; show JSON stdin on parse failures; catch Commander.js errors instead of silent exits. Why: to make root causes visible and diagnosable beyond the generic hook error.
  - Register recall script in package.json (bun recall): ensure the CLI is directly usable; previously the recall entry wasn’t registered.
  - Distinguish recall vs history: recall = search + LLM synthesis; history = index management, session/file recovery, and related features. Why: clarify scope, prevent feature drift, and set up complementary tools.
  - Plan integration between recall and history: design an integrated approach rather than keeping them completely separate. Why: unify workflows and leverage strengths of both tools.
  - Fix Commander exitOverride bug (help causing throws): plan to correct so help doesn’t crash the program. Why: improve CLI stability and UX.
  - Simplify CLI surface: 14 subcommands deemed too many; plan to rethink structure. Why: reduce complexity and improve maintainability.
  - Remove history.ts and migrate references to recall: consolidate architecture and reduce duplication. Why: avoid drift and simplify codebase continuity.

- Bugs found and root causes
  - Generic hook error (UserPromptSubmit) lacked detail: root cause was insufficient error propagation/diagnostics in the hook, hiding root causes like JSON parse failures or Commander issues.
  - Commander exitOverride causing help to throw: root cause was improper handling of Commander’s exit behavior when overridden; caused by using exitOverride with help flow.
  - Partial migration leaving history.ts references: root cause was incomplete refactor, leading to stale references and a fragmented codepath.

- Approaches that failed and why
  - Maintaining a large, multi-subcommand CLI (14 subcommands): too complex to maintain and reason about; prompted a rethink of the command structure.
  - Keeping history.ts separate while migrating features: added friction and ambiguity about tool scope; reinforced decision to unify under recall.

- Architectural patterns or conventions discovered
  - Consistent, verbose error logging pattern: start messages with a clear tag ([recall hook] FATAL:), include stack traces and timing, and reveal raw input when parsing fails.
  - Clear separation of concerns: recall for knowledge search/synthesis; history for index management and recovery; integration planned to leverage both.
  - CLI error handling with Commander: use exitOverride and explicit catch blocks, but requires careful handling to avoid crashes (fix planned).
  - Progressive refactor approach: move from parallel tools to a unified, simpler surface, with explicit migration steps (remove history.ts, rename references).

- Warnings for future sessions
  - Transient failures can mislead; ensure verbose logs are available to diagnose quickly.
  - Hook/CLI errors may surface intermittently; rely on stderr-rich outputs to pinpoint root causes.
  - Timeouts (e.g., synthesis timeout around 8s) can occur; consider tunable timeouts or progress indicators.
  - CLI surface complexity risk; reconsider the number and naming of subcommands to maintain usability.
  - Migration risks when removing history.ts; ensure all references and workflows are consistently updated to the new recall-centric structure.

## Session f3ecdd6e (14:05)

- Decisions made and WHY
  - Implement per-tool hard timeouts to prevent hangs and provide predictable budgets (server.ts, etc.). Reason: avoid indefinite waits and catch issues early.
  - Read terminal content from xterm.js buffer (window.term.buffer.active) instead of document.body.innerText. Reason: xterm.js renders to canvas; innerText is empty and won’t reflect real output.
  - Inline terminal-reading logic in each page.evaluate call (instead of defining readTerminalText in Node). Reason: page.evaluate runs in the browser context and cannot access Node-defined functions.
  - Enable browser/session auto-recovery and dead-session auto-cleanup. Reason: keep automation robust across crashes and restarts.
  - Restart MCP server after code changes to ensure updates are loaded. Reason: changes aren’t picked up until a fresh server/process is spawned.
  - Treat TTY robustness issues and TUI rendering issues as separate concerns. Reason: a blank TUI can be a rendering/PTY issue, not a TTY-robustness problem.

- Bugs found and root causes
  - Reading terminal output via document.body.innerText failed because xterm.js renders to canvas. Root cause: no text content in the DOM for canvas-based rendering.
  - Exposing a Node-defined readTerminalText function to the browser context failed due to serialization/scope boundaries. Root cause: page.evaluate runs in browser context; functions defined in Node aren’t accessible there.
  - TUI appeared blank outside a TTY; PTY not provided in non-tty environments. Root cause: no real PTY attached, so initial render can be empty.
  - Wait logic relied on DOM text; needed to switch to xterm.js API for content retrieval. Root cause: mismatch between rendering method and content-reading approach.

- Approaches that failed and why
  - Using innerText as a proxy for terminal output. Why it failed: xterm.js canvas rendering doesn’t populate DOM text.
  - Sharing a terminal-reading function between Node and browser contexts. Why it failed: function scope/context mismatch due to serialization.

- Architectural patterns or conventions discovered
  - Three-layer fix pattern:
    - Tool-level timeouts for robustness (budgeted waits).
    - Browser/session auto-recovery and dead-session cleanup for stability.
    - Direct reading from xterm.js buffer API for accurate terminal content.
  - Operational hygiene:
    - Kill stale processes and clean up orphaned ttyd processes to free port ranges.
    - MCP server auto-restarts on next tool call to pick up changes.
  - Clear separation of concerns:
    - TTY robustness (timeouts, reliable content reading) vs TUI rendering issues (canvas/PTY environment).

- Warnings for future sessions
  - Always read terminal content from xterm.js buffer when using ttyd with canvas; don’t rely on DOM text.
  - If using page.evaluate, inline the browser-context logic rather than trying to reuse Node-defined helpers.
  - Expect blank output outside a real PTY; verify PTY attachment when diagnosing rendering issues.
  - Watch for port/daemon hygiene: orphaned ttyd processes can block ranges; clean up proactively.
  - After code changes, expect to restart MCP server to ensure new logic is active.
  - Treat TUI rendering problems as potentially orthogonal to TTY robustness; investigate canvas rendering and PTY lifecycle separately.

## Session 87101cd9 (14:14)

- Decisions made and WHY
  - Enforce repo hygiene by removing dist and node_modules from version control; .gitignore was insufficient to prevent tracking them.
  - Add missing LICENSE files (inkx, chalkx) and fix metadata gaps (flexx: repository/homepage in package.json) to satisfy licensing and metadata requirements.
  - Rewrite/relocate README content for inkx and chalkx; README currently overpromises/overloads content or misaligns with package naming; goal: accurate, scannable docs.
  - Move bloated or ANSI/reference content from chalkx README into docs/ (and reduce overall length) to improve readability and maintainability.
  - Fix cross-package references (inkx link) to point at the correct repo, improving documentation integrity.
  - Standardize documentation naming to lowercase kebab-case; rename inkx PERFORMANCE.md to performance.md to enforce consistency.
  - Update plan/descriptions to reflect the above findings (e.g., inkx GitHub description, chalkx description) to improve project messaging.

- Bugs found and root causes
  - dist/ and node_modules accidentally tracked in git for flexx (62 dist files + 10 node_modules) and chalkx (38 dist + 2 node_modules); root cause: basically empty .gitignore and missing hygiene rules.
  - Missing LICENSE files for inkx and chalkx; root cause: licensing omissions.
  - flexx package.json missing repository/homepage fields; root cause: incomplete metadata.
  - chalkx README title inconsistency (title shows @beorn/chalkx while package name is chalkx); root cause: naming mismatch.
  - inkx README included overlong, non-targeted content; not a "bug" per se but indicates misalignment between docs and capabilities.
  - inkx PERFOR­MANCE.md used uppercase; inconsistent with doc naming conventions.
  - inkx link referenced nickvdyck/inkx instead of beorn/inkx; broken link.

- Approaches that failed and why
  - Relying on README as the primary source of truth for capabilities led to undersell/overload and misalignment with actual features; failed to communicate scope clearly.
  - Keeping ANSI references and a long roadmap in README reduced readability and maintainability; needed relocation to docs.
  - Inconsistent doc naming across packages caused friction; required a unification pass to kebab-case.
  - Delayed license/metadata checks allowed hygiene issues to persist; necessary to enforce earlier.

- Architectural patterns or conventions discovered
  - Five-phase rendering pipeline (layout calculated before rendering) plus RenderAdapter abstraction enabling multi-target rendering (terminal, Canvas, DOM, etc.); layout-driven rendering reduces jank and enables cross-target reuse.
  - Elm-inspired runtime structure (pure reducer, explicit event scheduling) coexisting with React-based rendering; combines disciplined state/effects with React rendering where appropriate.
  - Documentation strategy: central docs/ folder with navigation, separate design/api/usage docs, and consistent naming to improve discoverability and maintenance.
  - Documentation naming convention: majority kebab-case doc files; one-off exceptions should be renamed to fit the convention.

- Warnings for future sessions
  - Always verify license presence early; missing LICENSE files are a blocker for distribution and compliance.
  - Ensure package.json metadata (repository, homepage) is complete to avoid npm/registry issues.
  - Use and enforce a .gitignore policy to prevent dist and node_modules from being tracked; re-check before commits.
  - Keep README concise and aligned with capabilities; move technical depth to docs to avoid misrepresentation.
  - Maintain consistent cross-package links; verify links after edits to prevent breakages.
  - Apply naming conventions consistently from the start (lowercase kebab-case for docs) to minimize rework.

## Session dee56809 (14:25)

- Decision: Merge history.ts and recall.ts into a unified recall CLI with six modular files (format, search, status, sessions, files, hooks) to improve cohesion and maintainability.
  - Why: Consolidate functionality under a single tool while keeping concerns separated.

- Decision: Implement a thin orchestration layer (tools/recall.ts) using Commander to coordinate modules.
  - Why: Simple, predictable entrypoint that delegates to modular components.

- Decision: Fix exitOverride crash by making no-args invocation show help and exit with code 0.
  - Why: Stabilize CLI UX and prevent runtime crashes.

- Decision: Delete legacy tools/history.ts after replacing with unified recall CLI.
  - Why: Avoid duplication and drift between old and new implementations.

- Decision: Update all references across skills, hooks, docs, and package.json to reflect the new recall CLI structure.
  - Why: Maintain consistency and prevent breakages.

- Bug found / root cause: json-mode test failure on worktree vs main caused by branch timing (branched before recent L3 migration commits).
  - Root cause: branch divergence; not caused by recall changes.

- Bug found / root cause: Pre-existing failures (86 tests across km-cli, km-tui, km-repl) not related to recall work.
  - Root cause: baseline in the worktree; not indicative of recall regressions.

- Bug found / root cause: Stale lock file during merge process.
  - Root cause: concurrent/previous process left a stale lock; resolved by retry wait.

- Bug found / root cause: Beads JSONL staged but not committed; pushed incomplete changes.
  - Root cause: missing commit step; fix by committing beads and session memory before push.

- Approach that failed and why: Treating all test failures as caused by the current changes without checking main baseline first.
  - Lesson: Always verify against main to separate pre-existing issues from new changes.

- Architectural pattern discovered: Clear modularization by feature area under tools/recall (format, search, status, sessions, files, hooks).
  - Pattern: a thin orchestrator delegating to small, focused modules; aligns with single-responsibility principle.

- Architectural/convention note: Unified recall CLI with dedicated modules for: formatting/parsing, full-text search + grep + LLM, dashboard, session management, file I/O, and hooks.
  - Benefit: easier testing, maintainability, and targeted improvements.

- Warnings for future sessions:
  - Distinguish pre-existing failures from changes; validate against main branch early.
  - Keep worktree and submodules in sync (git fetch, rebase) before deep investigations.
  - Be mindful of migrations (e.g., L3) causing unrelated test failures in detached worktrees.
  - Pay attention to git hooks and locks (bd sync, stale locks) to avoid interrupted flows.
  - Ensure all staged artifacts (e.g., beads, session memory) are committed before pushing.
  - Document refactors and file-level responsibilities to aid review.

- Noteworthy outcomes:
  - Recall tests pass 39/39.
  - Recall CLI now organized into 6 modular files with a clear responsibility map.
  - Merge completed and changes pushed; pre-push checks addressed.

## Session af493086 (14:27)

- Decisions and rationale
  - Always prefer TUI mode for tests; GUI/TTY used only for pixel-level verification when needed, because TUI is faster, more reliable, and catches character-level issues.
  - Use the Board Driver with getState for AI-driven exploration and fuzzing; state shape (cursor, selectedNodeId, dialogs, screen, commands) enables deterministic decision-making and invariant checks.
  - When bugs are found, create a bead (bug record) and add a test under apps/km-tui/tests, with .skip if not fixing now; ensures reproducibility and traceability.
  - Update skill/docs to reflect actual workflow, replacing deprecated scripts with direct commands (e.g., bun test:fuzz, TEST_VAULT=… bun vitest run …) to avoid confusion.
  - Document that real vault tests should be run as baseline, and fuzz tests may require config tweaks; helps distinguish sanity checks from fuzz exploration.
  - Prohibit ad-hoc fuzz scripts living in the repo; keep fuzz/testing workflows clearly separated and configurable.
  - If using fuzz tests, require specific env vars (FUZZ=1) and ensure vitest includes the fuzz test files; prevents silent misconfiguration.
  - When using Peekaboo, apply longer timeouts (e.g., 10s for mcp, 5s for others) to avoid hangs in unresponsive sessions.

- Bugs found and root causes
  - Fuzz-test crash: resolveNodeFsPath crashed with repo.data.getNode being null; root cause likely fuzz tests using createFakeRepo without a data property, causing an unhandled rejection.
  - Potential test-pattern mismatch: fuzz files (.fuzz.ts) not matched by vitest include pattern; root cause: misconfigured test runner pattern, not a code logic bug.

- Approaches that failed and why
  - Relying on explore script redirecting to vitest fuzz tests without aligning file extensions/patterns; failed due to test discovery mismatch.
  - Attempting to reuse or reference deprecated scripts in SKILL.md; failed due to out-of-date guidance and ambiguous commands.
  - In-place fuzz script experiments without clear separation or env gating; led to config drift and confusion about how to run fuzz tests.

- Architectural patterns / conventions discovered
  - Driver-based testing paradigm: createBoardDriver(repo, "board") exposes press, getState, and cmd metadata; enables AI-driven and fuzz-driven exploration.
  - State-driven decision loop: AI chooses next action from driver.getState(); state includes precise cursor location, dialogs, and available commands.
  - Verification contract for UI tests: ensure exactly one cursor, essential DOM/buffer elements exist, and view mode indicator cycles; provides a stable, invariant-focused test baseline.
  - Bead-based bug workflow: link bugs to tests with a formal reference (e.g., bead ID) to maintain traceability.

- Warnings for future sessions
  - Keep test file patterns aligned with the test runner (e.g., ensure fuzz files are included by vitest); otherwise, tests won’t run or will be skipped.
  - When fuzz testing, reliably set and check environment variables (FUZZ=1) to enable fuzz-mode behavior.
  - Baseline real vault tests first; fuzz tests may require configuration tweaks to pass, so separate concerns early.
  - Avoid embedding ad-hoc fuzz scripts in the repo; prefer externalized or clearly versioned fuzz workflows with explicit docs.
  - If using Peekaboo, be mindful of timeouts; default to conservative timeouts to avoid spurious hangs unless debugging a rendering issue (e.g., 5s for mcp, 10s for start-up).
  - Documentation changes should reflect actual workflows to prevent future drift (e.g., replace deprecated scripts with direct CLI invocations).
  - Root-cause risk area: fuzz/test infrastructure can expose null/undefined repo structures (e.g., missing repo.data.getNode); add guards or stricter fixtures to prevent unhandled rejections.

## Session ad236c79 (14:38)

- Decisions and Why:
  - Read specific files to nail design details before writing the plan. Why: to base the plan on accurate, concrete context.
  - Inspect the ui-reducer (Redux Toolkit with Immer) and its UIContext consumers. Why: to understand data flow and how UI state is consumed.
  - Check the board reducer in @km/board and the consumers of the actions object. Why: to map action usage and interactions across modules.
  - Design the plan using a Plan agent to synthesize all exploration findings. Why: to formalize and structure the plan from gathered context.
  - Create/update tracking beads for this work and reference the bead in the plan. Why: to establish traceability and linkage between work items and planning.

- Bugs Found and Root Causes:
  - None reported in this session.

- Approaches That Failed and Why:
  - None reported in this session.

- Architectural Patterns or Conventions Discovered:
  - UI state is managed with Redux Toolkit plus Immer (ui-reducer pattern).
  - Modular reducers with explicit consumers of actions and context (uiContext, board reducer, actions object).
  - Meta-workflow pattern: use of a Plan agent to synthesize findings, plus beads as tracking artifacts, with explicit linking between plan and bead.

- Warnings for Future Sessions:
  - User interruptions occurred (session cut short by user for tool use); plan workflows should gracefully handle interruptions and preserve or reattach planned artifacts (beads/plans) to resume later.
  - Ensure proper linkage and state consistency when creating/updating beads and referencing them in plans to avoid orphaned or lost traceability.

## Session 1f28ff58 (14:40)

- Decisions made and why
  - Make MEMORY.md warnings unavoidable by placing the --id/--parent constraint at the top of MEMORY.md, since it’s always loaded in the system prompt (ensures the warning is visible).
  - Surface hook failures via a persistent log and graceful degradation (no exit(1)) instead of crashing, so users can inspect errors without the CLI dying.
  - Use program.outputHelp() for bun recall with no args to ensure a clean, non-abrupt exit.
  - Extend recall review to surface hook issues by:
    - Adding a recentHookErrors field to ReviewResult.
    - Parsing /tmp/recall-hook.log in reviewMemorySystem.
    - Displaying hook errors in formatReviewOutput.
  - Centralize and improve observability by logging hook errors to a dedicated file and exposing them in review outputs.
  - Fix FTS5 escaping by adding #, @, ~ to the escape list (root cause of FTS5 syntax errors in prompts).
  - Increase TTY timeout (tty_start) from 10s to 15s to accommodate slower prompts/LLMs.
  
- Bugs found and root causes
  - FTS5 syntax error near "#": # was not escaped; caused hook crashes when prompts contained hashtags or code/comments. Root cause: escaping list didn't include # (also surfaced for @, ~).
  - Hidden hook errors due to default behavior exiting on unhandled errors; caused generic “UserPromptSubmit hook error.” Root cause: exit(1) on failures prevented graceful degradation.
  - Logging mechanism previously using Bun.write with append was incorrect, leading to unreliable log writes; root cause: flawed log append approach.
  - 0 session memory files reported by hook analysis; root cause not fully clear from transcript but indicates memory hook not persisting memory data in some runs.
  
- Approaches that failed and why
  - Relying on stderr for errors: Claude Code swallowed details, leaving a vague message. Remedy: log to a file and surface via review outputs.
  - Not integrating hook logs into the review workflow: lacked actionable diagnostics. Remedy: parse /tmp/recall-hook.log and display in UI.
  - Narrow escaping logic for FTS5 without centralized handling: led to intermittent syntax errors for common characters; remedy: expand the escaping set and ensure it’s tested.
  
- Architectural patterns or conventions discovered
  - Observability-first pattern: introduce a dedicated hook log file and integrate its contents into the recall review flow for actionable diagnostics.
  - Clear separation of concerns: diagnostic/logging (hook.log), memory prompt warnings (MEMORY.md), CLI UX (outputHelp vs exit), and data escape logic (FTS5 escaping) are modularized.
  - Centralized input sanitation for search: FTS5 escaping lives in a single place (db.ts:607) to prevent syntax errors across prompts.
  - Convergent warning strategy: warnings embedded in a consistently visible location (MEMORY.md) and mirrored in docs.
  - Graceful degradation as a design principle: system continues with partial results when hook/LLM steps fail, instead of hard failures.
  
- Warnings for future sessions
  - FTS5 escaping must cover evolving user-input characters; keep #, @, ~ and any new problematic chars in the escape list and test prompts containing them.
  - If LLM synthesis or hook processing slows, timeouts can reappear as “UserPromptSubmit hook error”; monitor and adjust timeouts or add deeper backoff/retry logic.
  - Memory hook reliability: 0 session memory files suggests memory persistence can fail; ensure remember hook writes are robust and monitored.
  - Logs growth and permissions: /tmp/recall-hook.log should be managed (size, rotation, permissions) to avoid clogging and ensure readability.
  - Tests should cover real-world prompts with hashtags and other special characters to prevent regressions in escaping and error handling.
  - Review outputs should consistently surface recentHookErrors; maintain backward-compatibility if log format changes.

## Session 2643366a (14:47)

- Decisions made and why
  - Wire the production entrypoint (tui.tsx) to use the new Board app wiring via createBoardApp().run() instead of render(), because Board.tsx reads from a Zustand store via useAppStore() that is only initialized by createApp()/createBoardApp. This prevents mount-time errors and blank screens.
  - Treat the bug as a wiring issue between the L3 migration (Board.tsx, createBoardApp) and the production startup path (tui.tsx).

- Bugs found and root causes
  - Root cause: L3 migration added useAppStore() and createBoardApp(), but tui.tsx still used the old render() path, so Board.tsx mounted without the proper app context, causing useAppStore() to fail or be silently swallowed, resulting in a blank screen.
  - Specific error observed: useApp must be used within createApp().run() which indicates the wrong runtime path was used (render() vs createApp().run()).
  - Missing wiring: tui.tsx never imports or calls createBoardApp(), so the required store context isn’t provided to Board.

- Approaches that failed and why
  - Tests focus on the driver/Board component in isolation and bypass the production startup path (runBoard/tui.tsx). This creates a “test double divergence” where production wiring bugs slip through because integration/production path tests don’t exist.
  - No integration or smoke tests exercised runBoard() or the actual tui.tsx startup, so the production entrypoint wasn’t validated by tests.

- Architectural patterns or conventions discovered
  - Clear separation between:
    - Production entrypoint tui.tsx (shell that starts the TUI)
    - Board components that consume a Zustand store via useAppStore()
    - BoardApp/createBoardApp wrapping (requires createApp().run())
    - Test driver path that wraps Board with StoreContext.Provider for component tests
  - Migration pattern: moving from render()-based startup to a createApp()/run() pattern requires all entry points to be updated in lockstep; mixing old and new APIs causes runtime context errors.
  - Store context pattern: components rely on a properly initialized store context; mismatch leads to crashes or silent failures.

- Warnings for future sessions
  - Always verify production startup paths after a migration: add or enable an integration/smoke test that runs runBoard() or bun km view to ensure the production entrypoint wires correctly.
  - Guard against test-driver vs production path drift: ensure migration touches production entrypoints (tui.tsx) in addition to components and tests.
  - Watch for runtime errors like “useX must be used within createApp().run()” and map them to cross-cutting wiring issues rather than component logic bugs.
  - Consider adding a lightweight test that imports runBoard() and executes it in a mocked terminal to catch similar wiring regressions early.

## Session 76aab571 (14:49)

- Decisions made and WHY
  - Add explicit debugging steps across bugs.md, tui/fix.md, and explore/repro.md to enforce recall history, root-cause analysis, and prevention beads; reason: standardize and strengthen the debugging discipline across workflows.
  - Introduce recall/search-before-action in multiple places (Step 1.5, Step 0) and require root-cause analysis after fixes; reason: reduce rework, improve learning from past sessions, and prevent recurrence.
  - Create and formalize a Quality Checklist item set (recall searched, root cause, detection gap, prevention bead) to embed discipline into the workflow; reason: codify accountability and measurable checks.

- Bugs found and root causes
  - NONE documented in this session; changes are process improvements, not runtime bugs with identified root causes.

- Approaches that failed and why
  - NONE reported; no failed debugging approaches were described in the transcript.

- Architectural patterns or conventions discovered
  - Recall-before-action pattern: always review prior sessions/history before debugging from scratch (Step 0/Step 1.5).
  - Root-cause-after-fix pattern: analyze why the issue happened and why tests missed it, then plan prevention (Step 6).
  - Prevention bead concept: create a lightweight, lasting artifact to prevent recurrence.
  - Quality Checklist as a binding contract: explicit items (recall search, root cause, detection gap, prevention bead) to be completed.
  - Anti-patterns codified: fixing without understanding; closing without analyzing test gaps.
  - Cross-file consistency: similar conventions applied across bugs, UI fixes, and repro exploration to maintain a cohesive debugging discipline.

- Warnings for future sessions
  - Always perform recall/search of prior sessions before new debugging; do not skip history checks.
  - Complete root-cause analysis after fixes and document why tests missed the issue.
  - Explicitly create and reference a prevention bead to guard against regressions.
  - Do not close issues without addressing detected test gaps; ensure all items in the quality checklist are satisfied.

## Session 77150a55 (14:52)

- Decision and rationale
  - Implement an explicit --output <file> flag to write the full response to a file because stdout in background tasks truncates (≈30k chars) and large deep-research outputs exceed stdout limits.
  - Rationale: ensure agents can read complete results via the Read tool without relying on truncated stdout or deleted partial files.

- Root causes / bugs discovered
  - Partial file is deleted on completion (openai-deep.ts:206), causing loss if stdout is truncated mid-flight.
  - stdout streaming to process.stdout.write() combined with background task execution leads to truncation for large outputs (50k–100k+ chars).
  - Persistence writes incrementally to a partial file but lacks a robust recovery path when the stdout stream is truncated or the process ends prematurely.

- Approaches that failed and why
  - Relying on stdout streaming alone for large, long-running outputs in non-interactive/background contexts: fails due to strict stdout size limits.
  - Expecting the incremental partial file to survive after completion: fails because of automatic deletion on completion, losing any unread partial content.

- Architectural patterns / conventions revealed
  - Clear separation between streaming output (stdout) and persistent storage (partial file) is essential but currently fragile when the two interact in background contexts.
  - Prefer an explicit output artifact (file) for large results, decoupling runtime streaming from persistence and enabling reliable post-run reads via a Read tool.
  - Use explicit flags to control output destinations rather than implicit dependencies on stdout/persistence lifecycle.

- Warnings for future sessions
  - Avoid relying on stdout length when the process may run in non-interactive/background environments.
  - Do not delete partial artifacts mid-run; ensure safe retention or implement recovery/readback strategies if the process terminates unexpectedly.
  - Document environment constraints (stdout limits, background task behavior) and validate across interactive vs. background executions.
  - Consider robust, testable patterns for large outputs: file-based output, resumable streaming, and explicit cleanup only after successful completion.
