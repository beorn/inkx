# Obsidian-style %% comments

Obsidian's native comment syntax uses `%% ... %%` and only appears in
preview-hidden output. They're first-class in many vaults.

%% This is a top-level Obsidian comment %%

## Daily notes

Today's plan:

- Review PRs %% need to ask about the storage bead %%
- Write the RFC
- %% deferred — waiting on design input %% Ship the migration

%%
Multi-line Obsidian comment that spans multiple lines.
Often used to park thoughts while drafting.
%%

Wrapping text before a block comment.

%% block-style comment with no surrounding content %%

Wrapping text after.
