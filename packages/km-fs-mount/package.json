{
  "name": "@km/fs-mount",
  "private": true,
  "description": "Filesystem mount layer for km — extracts fs/, watch/, and fs-backed stores from @km/storage so web/canvas targets can consume @km/storage without node:fs.",
  "type": "module",
  "module": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@km/core": "workspace:*",
    "@km/markdown": "workspace:*",
    "@km/storage": "workspace:*",
    "alien-signals": "^3.1.2",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@beorn/watcher-chaos": "workspace:*",
    "vimonkey": "workspace:*"
  },
  "_note": "MUST stay private until the @km/fs-mount ↔ @km/storage source-level cycle is resolved (option c: extract a dep-free @km/runtime package). See packages/km-fs-mount/CLAUDE.md § Known constraint and hub/km/storage-architecture.md §6."
}
