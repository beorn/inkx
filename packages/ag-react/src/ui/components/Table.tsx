/** Compatibility path for the canonical generic Table. */
export { Table } from "../../components/Table"
export type {
  Column,
  Column as TableColumn,
  InteractiveTableProps,
  TableProps,
  TableRowId,
} from "../../components/Table"
